CHANGELOG
=========

v0.3.0 — 2026-03-12
--------------------

Bug fixes:
  - Fixed "Input type not supported" errors caused by run-external returning nothing
    - All parsed commands now use `do { ^cmd } | complete` for reliable output capture
    - Added null-safe field access: `($result.stdout? | default "") + ($result.stderr? | default "")`
    - Added empty-output guard: `if ($raw | str trim | is-empty) { return [] }`
  - Fixed "can't convert nothing to boolean" when a positional arg name collides with a flag name
    - Renamed `formula?` positional to `name?` in brew list/info/deps to avoid shadowing `--formula` flag
  - Fixed base command `--help (-h)` showing Nushell's built-in help instead of tool help
    - Nushell reserves `--help`; changed to `--info (-i)` for structured help output

Improvements:
  - Base command now returns help as a single structured table (columns: section, command, description)
    instead of printing multiple tables — output is filterable: `nubrew -i | where section == "Commands"`
  - Base command accepts `...rest` for pass-through to unwrapped subcommands (e.g. `nubrew doctor`)
  - Updated /to_nu and /nu_create_toml_from_posix_command templates with all fixes
  - README updated with structured help examples, expanded compatibility notes

v0.2.0 — 2026-03-12
--------------------

New examples:
  - examples/brew/brew.toml + brew.nu: Homebrew wrapper (list, info, search, outdated, deps, services)
  - examples/pip/pip.toml + pip.nu: pip3 wrapper (list, show, freeze, install, check)

Improvements:
  - README redesigned with Nushell project intro, disclaimer, icons, usage examples, and examples table
  - Expanded Docker example: added stats, top, events, port, version, info, history, inspect, diff subcommands

v0.1.0 — 2026-03-12
--------------------

Initial release.

Commands:
  - /to_nu: Generate Nushell def blocks from a TOML spec file
  - /nu_create_toml_from_posix_command: Auto-generate TOML specs from any CLI tool

Features:
  - TOML-driven spec format for describing CLI tool subcommands, flags, and output parsing
  - Multiple parsing strategies: tabular (detect columns), simple format patterns, regex, CSV/delimiter, line-oriented, and raw passthrough
  - Header sanitization — multi-word headers like "CONTAINER ID" become "container_id"
  - Stderr capture via `complete` for tools that write output to stderr
  - Streaming mode support — follow/watch flags stream raw, non-follow parses into structured tables
  - String flags use `= ""` defaults to avoid Nushell missing_flag_param parser errors
  - Simple `parse "{col}"` format preferred over regex to avoid Nushell angle-bracket parser issues
  - `detect columns --guess` for automatic column width detection
  - Column type coercion support
  - Row reversal support
  - Interactive TOML generator asks user for full vs specific subcommands and flag scope

Examples:
  - examples/docker/docker.toml: Docker wrapper spec (ps, images, logs)
  - examples/docker/docker.nu: Generated Nushell commands for Docker
  - examples/openfang/openfang.toml: OpenFang wrapper spec (33 subcommands)
  - examples/openfang/openfang.nu: Generated Nushell commands for OpenFang
